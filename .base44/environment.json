{
  "previewPort": 3000,
  "healthPath": "/api/health",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "JWT_SECRET",
      "description": "Secret key used to sign JWT authentication tokens. Generate a random 32+ character string.",
      "requiredAtBoot": true,
      "generatedForDevelopment": true
    }
  ]
}
